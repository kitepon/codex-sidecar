# codex-sidecar 0.3.10 registry provenance

- Date: 2026-08-30
- npm publication commit: `d6a9b01fd6727167ac074f5646281c95da83a120`
- Purpose: restore the missing immutable GitHub tag and Release without guessing a source commit

`npm view` reports the same `gitHead` for `codex-sidecar-core@0.3.10`,
`codex-sidecar-cli@0.3.10`, and `codex-sidecar-mcp@0.3.10`. The commit exists in the
canonical repository, is an ancestor of `origin/main`, and records the 0.3.10 release change.

The missing `v0.3.10` tag and GitHub Release may therefore be restored at this exact commit.
This evidence does not mark those external records complete; they must be queried after creation.
