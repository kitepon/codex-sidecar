{
  "name": "codex-sidecar",
  "version": "0.3.10",
  "private": true,
  "description": "Shared Codex sidecar agent bridge for reviews, exploration, and scoped work.",
  "license": "MIT",
  "author": {
    "name": "Quo / クオ at kitepon.dev",
    "url": "https://kitepon.dev/"
  },
  "homepage": "https://github.com/kitepon/codex-sidecar#readme",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kitepon/codex-sidecar.git"
  },
  "bugs": {
    "url": "https://github.com/kitepon/codex-sidecar/issues"
  },
  "type": "module",
  "scripts": {
    "build": "corepack pnpm -r build",
    "typecheck": "corepack pnpm --filter codex-sidecar-core build && corepack pnpm -r typecheck",
    "lint": "corepack pnpm -r lint",
    "test:release-gate": "node --test scripts/verify-release-commit.test.mjs",
    "test": "corepack pnpm --filter codex-sidecar-core test && corepack pnpm --filter codex-sidecar-cli test && corepack pnpm --filter codex-sidecar-mcp test"
  },
  "devDependencies": {
    "@types/node": "^22.15.3",
    "typescript": "^5.8.3"
  },
  "packageManager": "pnpm@10.10.0"
}
